<%= form_for(@type_room) do |f| %>
  <% if @type_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_room.errors.count, "error") %> prohibited this type_room from being saved:</h2>

      <ul>
      <% @type_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
